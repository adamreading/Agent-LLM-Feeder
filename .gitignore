node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
*.bak
*.vsix
.env
.env.local
.DS_Store

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh
.claude/settings.json

# Runtime server log (created by the detached launch; never track it)
server/server.log
*.log

# Agent UI output files (generated .md/.txt/.pdf, downloadable via the Agent tab)
tmp/

# Machine/fleet briefs — local only; public repo is world-readable forever. Shareable copies live in *.example.md
CLAUDE.md
TEAM-ONBOARDING.md
